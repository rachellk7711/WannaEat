# 15. 데이터 아키텍처 도식

작성일: 2026-09-14 · 기준: `TRD.md` v0.1 · `scripts/01_schema.sql` v2

> 🔴 **이 문서 전체가 보류 상태다.** 데이터 아키텍처는 사용자가 직접 검토해 확정하기로 한 영역이다.
> 아래는 **지금까지 작성된 내용을 그림으로 옮긴 것**이며, 확정된 설계가 아니다. 검토 후 바뀔 수 있다.

---

## 1. 시스템 구성

```
┌─────────────────────────── 사용자 브라우저 (모바일 76~78%) ──────────────────────────┐
│                                                                                      │
│   [기준 설정 화면]        [이미지 선택·촬영]        [전송 전 확인]        [결과 화면] │
│         │                        │                       │                     ▲    │
│         │                        └──── 크기 조절 ────────┤                     │    │
│         │                                                │ 확인한 픽셀만        │    │
└─────────┼────────────────────────────────────────────────┼─────────────────────┼────┘
          │                                                │                     │
          │ ① 기준 저장·조회                                │ ② 판독 요청          │ ⑤ 결과
          ▼                                                ▼                     │
┌──────────────────────────┐              ┌────────────────────────────────┐     │
│  Cloudflare Pages        │              │  Supabase Edge Function        │     │
│  (정적 자산만)           │              │  ─ AI 프록시 + 판정 API ─       │─────┘
│  · HTML/CSS/JS 배포      │              │  · API 키는 여기에만 보관       │
│  · 무료·상업 이용 허용   │              │  · 월 50만 호출 / 150초 / CPU 2초│
└──────────────────────────┘              └───────┬───────────────┬────────┘
                                                  │ ③ 글자 읽기   │ ④ 조회·판정
                                                  ▼               ▼
                                        ┌──────────────┐  ┌───────────────────────┐
                                        │  Gemini      │  │  Supabase Postgres    │
                                        │  (유료 티어) │  │  (서울 리전 · RLS)     │
                                        │  · 텍스트만  │  │  · 제품 · 성분 · 사용자│
                                        │  · 판정 안 함│  └───────────────────────┘
                                        └──────────────┘
                                          이미지는 저장하지 않음
```

**보류 항목:** 호스팅 조합, Edge Function 분리 방식, 리전 설정.

---

## 2. 데이터 3층

```
┌─ A. 제품 층 ─────────────────────────────────────────── 공공DB에서 옴 ──┐
│                                                                          │
│   products  168.7만 행 · 329MB                                           │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │ 품목번호 │ 제품명 │ 업소 │ 유형 │ 원재료명(문자열 통째로) │ 순서 │    │
│   └────────────────────────────────────────────────────────────────┘    │
│   "이 제품에 뭐가 들었나"                     ← 제품명 입력 시에만 사용   │
└──────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 쉼표로 분해 → 토큰
                                    ▼
┌─ B. 성분 층 ─────────────────────── 공공 마스터 + 우리가 만든 것 ────────┐
│                                                                          │
│   ingredient_master   표준명 · 범주 · 용도 · 축 분류        58,142종 목표 │
│   ingredient_alias    알룰로오스→알룰로스, 채종유→카놀라유   수천 건      │
│   ┌──────────────────────────────────────────────────────────────┐      │
│   │ 식별층 A  전량      "이게 무엇인가"          자동 + 검수       │      │
│   │ 판정층 B  수백 종   모드 기본값               사람            │      │
│   │ 근거층 C  수십 종   각국 기준·출처            사람 (가장 무겁다)│      │
│   └──────────────────────────────────────────────────────────────┘      │
│   "그게 무엇인가"                              ← 두 경로 모두 사용       │
└──────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─ C. 사용자 층 ────────────────────────────────── RLS로 본인만 접근 ──────┐
│                                                                          │
│   user_criteria        성분별 피해요/알려만줘요                          │
│   user_criteria_state  미설정 / 모드 기본값 / 직접 선택 + 기준 버전      │
│   criteria_snapshot    확정 시점의 기준 전체 (이력에서 되짚기용)         │
│   analysis_history     판정일·제품명·결과·확인 불가 수·당시 기준 버전     │
│   product_alias        쇼핑몰 상품명 → 품목번호 (본인 전용 캐시)          │
│                                                                          │
│   "내 기준은 무엇인가"                                                   │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 판정 흐름 — 라벨 경로 (1차)

```
 사용자                브라우저              Edge Function          Gemini        Postgres
   │                      │                       │                   │              │
   │ 사진 선택            │                       │                   │              │
   ├─────────────────────►│                       │                   │              │
   │                      │ 크기 조절             │                   │              │
   │ 전송 전 확인 ◄───────┤ (원본은 안 나감)      │                   │              │
   │ [확인] 누름          │                       │                   │              │
   ├─────────────────────►├──── 확인한 픽셀 ─────►│                   │              │
   │                      │                       ├─── 이미지 ───────►│              │
   │                      │                       │                   │ 글자만 읽음  │
   │                      │                       │◄── 원재료 텍스트 ─┤ (판정 안 함) │
   │                      │                       │   순서 보존       │              │
   │                      │                       ├─ 토큰 분해 → 정규화 ─────────────►│
   │                      │                       │                   │  성분 사전   │
   │                      │                       │◄──────────────────────────────────┤
   │                      │                       │                                   │
   │                      │                       ├─ 내 기준과 대조 (고정 규칙)       │
   │                      │                       ├─ 판독 완전성 집계                 │
   │                      │                       ├─ 이력 저장 ──────────────────────►│
   │ 결과 ◄───────────────┤◄──────────────────────┤                                   │
   │                                                                                   │
   │  ┌──────────────────────────────────────────────────┐                            │
   │  │ 내 기준 대조   말토덱스트린 — 앞쪽 표시           │                            │
   │  │ 판독 상태      ⚠ 확인 불가 2개                    │                            │
   │  │                복합조미식품, 혼합제제             │                            │
   │  └──────────────────────────────────────────────────┘                            │
```

**이미지는 어디에도 저장되지 않는다.** 통과만 하고 버려진다.

---

## 4. 판정 흐름 — 제품명 경로 (보조)

```
 "[무료배송] 오리온 초코파이 정 12개입 1박스"
                  │
                  ▼  정규화 (프로모션 문구·수량·괄호 제거)
              "초코파이"
                  │
                  ▼  products 유사도 검색 (pg_trgm)
        ┌─────────┴─────────┐
        │  후보 3건          │
        │  ① 초코파이(오리온)│──► 사용자가 고름 ──► product_alias 에 저장 (본인 전용)
        │  ② 초코파이바      │                      다음부터 검색 없이 바로
        │  ③ 초코파이情(해태)│
        └────────────────────┘
                  │ 확정
                  ▼
        rawmtrl_nm + RAWMTRL_ORDNO
                  │
                  ▼  ORDNO로 정렬 → 표시 순서 복원
        [밀가루, 정제수, 설탕, …]
                  │
                  ▼  이후는 라벨 경로와 동일

 ⚠️ C003(건기식)·수입식품은 순서 컬럼이 없어 위치를 표시하지 않는다
 ⚠️ 자동확정률 30~50% (V2) — 그래서 보조 경로다
```

---

## 5. 테이블 관계 (현재 스키마 v2)

```
                    ┌──────────────────┐
                    │    products      │ 168.7만
                    │  prdlst_report_no│◄──┐ PK
                    │  prdlst_nm       │   │
                    │  rawmtrl_nm      │   │
                    │  rawmtrl_ordno   │   │
                    └────────┬─────────┘   │
                             │ 1:1          │
                    ┌────────▼─────────┐   │
                    │product_health_ext│   │ 건기식 부가정보
                    └──────────────────┘   │
                                           │
   ┌──────────────────┐                    │
   │ingredient_master │◄──┐                │
   │  std_name        │   │ FK             │
   │  category / axis │   │                │
   └────────┬─────────┘   │                │
            │ 1:N         │                │
   ┌────────▼─────────┐   │                │
   │ingredient_alias  │   │                │
   │  alias → std_id  │   │                │
   └──────────────────┘   │                │
                          │                │
   ┌──────────────────┐   │                │
   │   diet_modes     │◄─┐│                │
   └────────┬─────────┘  ││                │
            │ 1:N        ││                │
   ┌────────▼─────────┐  ││                │
   │mode_default_rules│──┘│ strength: 2단계│
   └──────────────────┘   │                │
                          │                │
  ═══ 사용자 영역 (RLS) ══│════════════════│═══════════════
                          │                │
   ┌──────────────────┐   │                │
   │  user_criteria   │───┘                │
   │  (user, ingr)    │                    │
   └──────────────────┘                    │
   ┌──────────────────┐                    │
   │user_criteria_state│  상태 + 기준 버전  │
   └────────┬─────────┘                    │
            │                              │
   ┌────────▼─────────┐                    │
   │criteria_snapshot │  버전별 기준 전체   │
   └────────▲─────────┘                    │
            │ criteria_version             │
   ┌────────┴─────────┐                    │
   │ analysis_history │────────────────────┘ (매칭 성공 시에만)
   │  판정일·제품명    │
   │  verdict          │
   │  unknown_count    │
   └──────────────────┘
   ┌──────────────────┐
   │  product_alias   │  scope='user' 기본
   └──────────────────┘
   ┌──────────────────┐
   │ingredient_feedback│ 미등록 성분 큐
   └──────────────────┘
```

**사용자 기준 (2026-09-18 변경):** `strength`는 `피해요 / 알려만줘요` 2단계 설정을 구분한다. 피해요는 발견 시 불일치에 포함하고, 알려만줘요는 발견 사실만 안내한다. 숫자 인코딩과 기존 SQL 제약 변경은 구현 시 확정하며 과거 이력의 기준은 보존한다.

**보류 항목:** 테이블 분할 방식, `result_json` 세부 스키마, 인덱스 구성.

---

## 6. 적재 파이프라인

```
 [식품안전나라]                    ※ 파일 다운로드는 시간 제한 없음
      │  CSV (로그인 필요 — 사용자 작업)
      ▼
 data/raw/     C002 · C006 · C003        (I1250은 참고용)
      │
      ▼  scripts/02_prepare_csv.py   ── 2패스
      │    1패스: 같은 품목번호인데 내용이 다른 행 탐지 → _conflicts.csv
      │    2패스: 충돌 품목번호는 통째로 제외, 동일 중복은 하나만
      ▼
 data/prepared/products.csv  168.7만 · 350MB
      │
      ▼  COPY (새 테이블 → 검증 → 이름 교체)
 Supabase products
      │
      └── 월 1회 전체 재적재 (증분 갱신 안 함)
```

**보류 항목:** 적재 방식(COPY vs 스마트싱크), 교체 절차, 용량 대응.

---

## 7. 성분 사전 구축 파이프라인 (진행 중)

```
 [우리 제품 DB]                      [공공 원재료 마스터]
 원재료 등장 1,080만                  I2520 43,612 · I1020 24,875
      │                                       │
      ▼ 토큰 분해·정규화                      ▼ 대표명 + 이명 전개
 58,142종 ───────────────┬─────────────► 표제어 50,048개
                         │
              ┌──────────▼──────────┐
              │  1·2단계 매칭 (완료) │
              └──────────┬──────────┘
                         │
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
   exact 18,556종    derived 15,789종   unmatched 23,721종
   등장 98.47%       등장 0.50%         등장 1.02%
        │                │                │
        └────────┬───────┘                ▼
                 │                ┌───────────────────┐
                 │                │ 3단계 LLM 분류     │ 🔄 진행 중
                 │                │ (범주만, 판정 아님)│ gemini-3.6-flash
                 │                └─────────┬─────────┘
                 │                          ▼
                 │                ┌───────────────────┐
                 │                │ 4단계 사람 검수    │ 당류·전분·감미료·유지만
                 │                └─────────┬─────────┘
                 ▼                          ▼
        ┌────────────────────────────────────────┐
        │  ingredient_master + ingredient_alias  │
        └────────────────────────────────────────┘
                         │
                         ▼  운영 중
              미등록 토큰 → ingredient_feedback → 월 1회 재분류

 현재 제품 완전 판독률:  81.8% (기반 3천종) → 96.33% (마스터 대조 후) → 목표 99.9%
```

---

## 8. 확정되지 않은 것 (검토 대기)

| # | 항목 | 현재 안 | 왜 보류인가 |
|---|---|---|---|
| 1 | 호스팅 구성 | Cloudflare Pages + Supabase Edge Functions | 무료·상업 이용 조건으로 고른 것. 실제 배포 경험 전 |
| 2 | 적재 방식 | CSV → COPY, 월 1회 전체 교체 | 용량 실측 전 |
| 3 | 용량·요금제 | Free로 시작, 초과 시 Pro | 168.7만 건 적재를 아직 안 해 봄 |
| 4 | 인덱스 구성 | 제품명 trigram 2개 + 유형·변경일 | 한글 trigram 동작 미확인 |
| 5 | `result_json` 세부 | 원재료 전사·출처·확인 불가 항목 | 화면 설계와 함께 정해야 함 |
| 6 | 제품 DB 축소 | 하지 않음 | 인덱스 포함 용량을 본 뒤 판단 |
| 7 | 건기식(C003) 포함 여부 | 포함 | 순서 정보가 없어 위치 안내 불가. 2단계 기준 대조는 발견 여부를 사용하므로 표시 순서와 별개로 검토 |

**이 7가지가 정해지기 전까지 `TRD.md`의 아키텍처 절은 제안이다.**

> 2026-09-17 — 검증 이후 드러난 스키마 변경 방향(기준 원재료 · 주장 단위 근거 · 원본 스냅샷 · 영양 레이어)은
> `23_스키마_변경초안.md` 에 따로 정리했다. 역시 검토 대기다.
