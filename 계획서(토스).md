# 알고먹을래? (WannaEat) 앱인토스 출시 계획서

| 항목 | 내용 |
|---|---|
| 작성 기준일 | 2026-09-18 |
| 국문 서비스명 | 알고먹을래? (콘솔 등록 전 확정한 표시 이름) |
| 문서 목적 | 기존 WannaEat 기획을 앱인토스 **비게임 WebView 미니앱**으로 출시하기 위한 제품·기술·운영 실행안 |
| 현재 상태 | `apps/wannaeat`에 녹색 A안, 분류 → 소분류 → 기준 선택, 피해요/알려만줘요, ID·버전 기반 기기 저장, Supabase Storage 공개 카탈로그·판정 규칙 읽기, 사진첩·카메라 미리보기, 영역 자르기·전송 전 확인·원재료 추출·사용자 수정·고정 규칙 대조 화면을 구현했다. 추출 함수는 배포돼 실제 라벨 사진으로 동작을 확인했다. 현재 카탈로그는 6개 분류·14개 소분류·62개 기준이다. Edge Function 배포와 Gemini 비밀 환경 변수 설정, 사용자 기준의 서버 저장·자르기·계정 연동은 아직 없다. 아래 남은 계획은 출시 준비 완료 선언이 아니다. |
| 제품 기준 문서 | [`docs/PRD.md`](docs/PRD.md), [`docs/TRD.md`](docs/TRD.md), [`docs/21_검증_최종보고서.md`](docs/21_검증_최종보고서.md), [`docs/22_기준설정_분류_제안.md`](docs/22_기준설정_분류_제안.md) |

## 1. 결론: 어떤 형태로 만들 것인가

**토스 앱 안에서 완결되는 비게임 WebView 미니앱**으로 만든다. React + TypeScript 기반 CSR/정적 번들을 앱인토스 WebView SDK 3.x로 빌드하여 `.ait` 파일을 콘솔에 업로드한다. API·DB·AI 호출은 별도 백엔드에서 처리한다. 기존 계획의 Cloudflare Pages는 미니앱 화면의 배포처로 사용하지 않는다. 앱인토스 [비게임 출시 가이드](https://developers-apps-in-toss.toss.im/checklist/app-nongame)는 최종 화면을 토스 도메인에서 렌더링하고 SSR 및 자사 사이트로의 화면 이동·중첩 WebView를 금지한다. Cloudflare는 별도 소개·약관 페이지가 필요한 경우에만 검토한다.

첫 출시는 **무료 원재료 표시 대조 서비스**로 범위를 고정한다. 사용자가 원재료 기준을 직접 선택·수정하고, 라벨 사진 또는 스크린샷을 올려 표시된 원재료가 본인 기준과 일치하는지 확인한다. 식단 프리셋은 적용하지 않는다. AI는 원재료 글자를 추출하는 데만 사용하고, 대조는 고정 규칙으로 실행한다. 결과는 `기준 불일치 / 기준 내 / 판단 보류`와 판독 상태를 분리하여 보여준다. **알레르기·알레르겐 판정, 바코드 입력, 장바구니 일괄 분석, 제품 등급·안전 판정은 첫 출시 범위에서 제외**한다. 범위 근거는 내부 [PRD](docs/PRD.md)와 [2026-09-17 검증 보고서](docs/21_검증_최종보고서.md)다.

### 2026-09-18 기준 설정 구현 결정

- 공통 목록은 `data/prepared/criteria_catalog.json`의 검증된 스냅샷을 번들에 넣고, Supabase Storage `wannaeat-reference`에서 버전별 JSON으로 배포한다. 파일 체크섬을 확인하고, 통신 실패 시 기기의 최근 목록 또는 번들을 사용한다. 관계형 테이블과 사용자 클라우드 저장은 아직 연결하지 않았다.
- 사용자 목록은 `{rulesetVersion, selections: [{kind: 'criterion' | 'subgroup', id, strength}]}`로 저장한다. `strength`는 `avoid`(피해요), `inform`(알려만줘요)만 사용한다.
- `selectAll: true`인 소분류만 전체 선택한다. 개별 변경이 전체 선택보다 우선하며, 하나를 해제하면 나머지를 개별 기준으로 풀어 보존한다.
- `includedIn`은 표기의 동시 출현에 따른 참고 관계이므로 선택을 자동으로 추가·삭제하는 데 사용하지 않는다. 검색어도 실제 성분 판정 규칙으로 사용하지 않는다.
- 날짜와 내용 해시로 버전을 구분한다. 목록 변경 시 사용자에게 재확인을 받고, 과거 이름 기반 설정을 임의 변환하지 않는다.
- 현재 버전은 기준 목록에 대한 버전이다. 실제 판정 엔진 연결 시 사용한 규칙 버전까지 함께 고정해야 한다. 구현·갱신 절차는 [앱 README](apps/wannaeat/README.md)에 기록한다.

### 2026-09-18 사진 이후 흐름 구현 결정

`사진 선택 → 보낼 영역 자르기 → 전송 전 확인 → Edge Function의 원재료 텍스트 추출 → 사용자 수정 → 고정 규칙 대조 → 결과`로 구성한다. 보낼 픽셀은 브라우저에서 잘라 1600px JPEG 로 다시 그리고, 그 그림을 그대로 보여준 뒤 동의를 받는다. 원본은 전송하지 않는다. AI에는 성분 판단을 맡기지 않고, 사진에서 인쇄된 원재료만 읽도록 한다. 사진은 저장하지 않는다. 읽은 원재료·그때의 기준·결과는 **기기 안에만** 최근 30건 남기고(`wannaeat.history.v1`), 사용자가 한 건씩 또는 전체를 지울 수 있다. 서버로는 보내지 않는다. 결과에는 `표기에 발견 / 확인 필요 / 확인 불가 / 표기에 없음`을 구분하고, 이는 식품 안전성·알레르기·함량·건강 영향을 판단하지 않는다는 한계를 함께 표시한다.

추출 모델은 **`gemini-3.1-flash-lite` + `MEDIA_RESOLUTION_LOW`** 를 기본으로 쓴다(실측: 라벨 한 장 입력 354토큰·출력 103~174토큰, 작은 글씨 원재료 24종 전부 판독). 읽어내지 못하면 같은 모델에서 한 단계 자세히 보고, 모델 자체가 막히면 3.5 Flash-Lite·3.6 Flash 로 넘어간다. 서버 함수는 Gemini 키를 Edge Function Secret에서만 읽는다. 사용자 브라우저에는 공개 Supabase 키만 둔다. 함수 배포에는 프로젝트 소유자의 Supabase CLI 로그인 또는 배포 토큰이 필요하다. 출시 전에는 토스 익명키 검증, 영속형 요청 제한, 이미지 전송 고지·보존 정책을 실제 운영 환경에서 확정한다.

### 서비스 흐름

```text
토스 진입 → 서비스 설명 → 내 기준 설정/확인
          → 사진첩 선택 또는 카메라 촬영 → 전송 영역 확인·자르기
          → 원재료 추출 → 추출 결과 확인/수정 → 고정 규칙 대조
          → 결과 + 판독 한계 + 출처 → 이력 저장/재확인
```

처음 진입하자마자 로그인·권한 동의 바텀시트를 띄우지 않는다. 카메라와 사진첩 권한은 해당 버튼을 누를 때 요청하며, 거부해도 제품명 검색이나 직접 입력 등 남은 경로를 사용할 수 있도록 한다. 이는 [비게임 UX·권한 체크리스트](https://developers-apps-in-toss.toss.im/checklist/app-nongame)에 따른다.

## 2. 기존 계획에서 바꿔야 하는 사항

| 기존 문서의 가정 | 앱인토스용 결정 | 이유·확인 근거 |
|---|---|---|
| Cloudflare Pages에 사용자용 웹앱 배포 | 앱 화면은 SDK 번들로 토스에 업로드 | [비게임 출시 가이드](https://developers-apps-in-toss.toss.im/checklist/app-nongame)의 토스 도메인 렌더링·SSR 제한 |
| Supabase Auth로 로그인 | 첫 무료 버전은 `User.getAnonymousKey()`로 식별. 회원 연동이 필요할 때만 **토스 로그인** 도입 | 미니앱 내 로그인은 토스 로그인만 허용됨. [서비스 오픈 정책](https://developers-apps-in-toss.toss.im/intro/guide), [익명키 SDK](https://developers-apps-in-toss.toss.im/documentation/sdk/domains-api/user/user.getanonymouskey) |
| 기존 PG/토스페이 정기결제로 디지털 구독 | **해당 없음 — 무료 서비스로 확정(2026-09-18).** 유료화하려면 사업자 등록과 인앱 결제(IAP)가 필요하다 | 디지털 상품은 IAP만 허용됨. [서비스 오픈 정책](https://developers-apps-in-toss.toss.im/intro/guide), [IAP 구독 문서](https://developers-apps-in-toss.toss.im/documentation/common/monetization/iap/in-app-subscription) |
| 베타 후 자동 결제 없는 구독 | **해당 없음.** 결제 UI 자체를 넣지 않는다 | 기존 PRD의 “자동 결제 없음”과 IAP 정기구독은 그대로 양립하지 않음. 무료 베타에는 결제 UI를 넣지 않음. |
| 일반 웹 파일 선택·카메라 호출만 상정 | `Device.getPhotos`, `Device.openCamera`와 권한 흐름을 실기기 검증 | [사진첩 SDK](https://developers-apps-in-toss.toss.im/documentation/sdk/domains-api/device/device.getphotos), [카메라 SDK](https://developers-apps-in-toss.toss.im/documentation/sdk/domains-api/device/device.opencamera) |
| 서버 RLS로 사용자 행 보호 | **익명키 자체를 DB 권한으로 취급하지 않음**. 서버에서 토스 키 검증 후 자체 세션을 발급하고 소유권 검증; DB는 직접 접근 차단 또는 검증된 JWT 기반 RLS | 토스 [익명키 검증 API](https://developers-apps-in-toss.toss.im/api/user-key)는 mTLS 서버 호출. 인증 설계를 확정한 뒤 RLS 구현 |

이력은 기기 안에만 저장한다. **기기를 바꾸거나 앱을 지우면 이력은 남지 않는다.** 서버 동기화는 토스 로그인이 필요하고, 토스 로그인은 사업자 등록이 필요해 지금 범위 밖이다.

## 3. 기술 설계

### 3.1 프런트엔드와 번들

- React + TypeScript + Vite 프로젝트를 `apps/wannaeat`에 생성했다. 앱 식별자 `wannaeat`은 콘솔 등록 전 확정해야 한다.
- `@apps-in-toss/web-framework` **3.x**를 설치하고 `apps-in-toss.config.ts`에 `appName`, `brand.primaryColor`, `webView`, `permissions`, `webBundleDir`를 설정한다. 빌드 스크립트는 웹 빌드 후 `ait build`를 실행하여 `.ait`를 생성한다. 최신 설정 구조는 [SDK 3.x 문서](https://developers-apps-in-toss.toss.im/documentation/integration/sdk-3.x)를 기준으로 한다. 기존 [웹 연동 튜토리얼](https://developers-apps-in-toss.toss.im/ai-vibe-coding/tutorials/webview)의 `granite.config.ts` 예시는 3.x 기준보다 오래되었으므로 복사하지 않는다.
- 토스 내비게이션 바, 뒤로가기, Safe Area, 라이트 모드, 큰 글씨·터치 영역을 반영한다. `@toss/tds-mobile`과 `@toss/tds-mobile-ait`는 앱인토스와 호환되는 버전으로 고정하고 실기기에서 확인한다. [비게임 체크리스트](https://developers-apps-in-toss.toss.im/checklist/app-nongame), [TDS 소개](https://developers-apps-in-toss.toss.im/design/components).
- 첫 화면은 “원재료 표시를 내 기준과 대조”라는 목적을 바로 설명한다. 콘솔의 `앱 내 기능`에는 출시 번들에서 실제 완료되는 기능만 등록하고, 등록한 딥링크 경로로 직접 진입할 수 있게 한다. [주요 기능 등록](https://developers-apps-in-toss.toss.im/guide/operation/function).
- 사진은 클라이언트에서 자르기·크기 조정 후 사용자가 전송을 확인한 영역만 HTTPS로 보낸다. 이미지 원본은 서버에 저장하지 않는다. 업로드 실패, 권한 거부, 작은 글자, 잘못된 사진의 복구 경로를 제공한다.

### 3.2 백엔드·인증·데이터

```text
앱인토스 WebView 번들 ──HTTPS──> 자체 API/Edge Function ──> Postgres
          │                              ├──> Gemini (OCR/전사 전용)
          │                              └──> 토스 서버 API (필요 시, mTLS)
          └── SDK: 익명키·사진첩·카메라·분석 이벤트
```

- 기존 Supabase Postgres와 Edge Functions는 후보로 유지한다. 다만 토스 서버 API는 **파트너 서버의 mTLS 인증서**가 필요하므로, 선택한 실행 환경이 클라이언트 인증서·개인 키를 안전하게 제공하는지 먼저 작은 연동 실험으로 검증한다. 불가능하면 mTLS 호출 전용 서버 또는 게이트웨이를 둔다. [API 인증](https://developers-apps-in-toss.toss.im/api), [서버 API 이용하기](https://developers-apps-in-toss.toss.im/documentation/integration/server-api).
- 첫 실행에서 `User.getAnonymousKey()`의 해시를 받아 서버로 보내고, 서버가 토스의 검증 API로 유효성을 확인한 후 짧은 수명의 자체 세션을 발급한다. 매 요청에 해시 문자열만 제시하면 타인 키 재사용을 막을 수 없는 설계가 되므로, 세션 발급·재검증·오용 제한·키 저장 방식을 보안 설계 단계에서 확정한다. 민감한 서버 키·Gemini 키·mTLS 개인 키는 클라이언트 번들에 넣지 않는다.
- 사용자 기준·분석 이력은 **기기에만** 둔다. 서버에는 사용량 집계만 쌓고 공개 키로는 접근할 수 없다. 나중에 서버 저장이 필요해지면 서버가 확인한 사용자 ID로만 읽고 쓰고, RLS 교차 계정 테스트를 한다.
- 제품·성분 사전은 출처·갱신일·검증 상태를 분리해 관리한다. `evidence_candidates.csv`를 곧바로 사용자 카드나 판정에 노출하지 않는다. 내부 [검증 보고서](docs/21_검증_최종보고서.md)의 미해결 F6~F13과 원재료 식별 문제를 출시 게이트로 둔다.
- 분석 API는 `추출 원문 → 사용자 확인본 → 표준명 후보 → 확정 매칭 → 기준 대조 → 판독 완전성`을 구분해 기록한다. 모호한 제품명 매칭·미등록 원재료·하위 원재료 미표시는 `판단 보류` 또는 `확인 불가`로 표시한다. 제품별 함량·안전성을 추론하지 않는다.
- AI가 사용자에게 직접 만든 설명을 노출한다면 생성형 AI 사전 고지와 결과 표시가 필요하다. 현 설계처럼 AI를 **전사에만** 사용하더라도 이미지 전송과 자동 추출 사실을 사용자에게 알리고, 사람이 수정할 수 있게 한다. UI 문구에 대한 정책 적용 여부는 검수 전 재확인한다. [생성형 AI 정책](https://developers-apps-in-toss.toss.im/intro/guide).

### 3.3 Origin·네트워크

2026-08-25 릴리즈 노트는 **SDK 3.1.1부터** WebView Origin이 다시 `*.apps.tossmini.com`으로 바뀌었다고 명시한다. 따라서 현재 신규 3.x 번들의 백엔드 CORS에는 아래 두 Origin을 등록하고, QR·라이브에서 실제 `Origin`을 기록해 재확인한다. [릴리즈 노트](https://developers-apps-in-toss.toss.im/release-note). 일부 [배포 문서](https://developers-apps-in-toss.toss.im/guide/operation/deploy)와 [마이그레이션 문서](https://developers-apps-in-toss.toss.im/documentation/integration/sdk-3.x)에는 변경 전 `*.web.tossmini.com` 값이 남아 있다.

```text
https://<appName>.private-apps.tossmini.com  # 콘솔 QR 테스트
https://<appName>.apps.tossmini.com          # 실제 서비스
```

CORS는 정확한 Origin만 허용하고, API는 HTTPS만 사용한다. 개발용 localhost는 개발 환경에만 넣는다. 서버 API의 응답은 HTTP 상태만 보지 말고 `resultType`도 확인한다. [서버 API 문서](https://developers-apps-in-toss.toss.im/documentation/integration/server-api), [익명키 검증 API](https://developers-apps-in-toss.toss.im/api/user-key).

## 4. 제품 화면과 검수용 시나리오

| 화면 | 첫 출시 필수 동작 | 실패·경계 처리 |
|---|---|---|
| 시작 | 서비스 목적, 지원 범위, 사진 처리 안내, 기준 설정으로 이동 | 로그인 강제 팝업 없이 진입 |
| 내 기준 | 모드 기본값 제시 → 사용자가 성분별 `피해요 / 알려만줘요` 2단계 확정, 출처 카드 보기 | 설정 변경 전후 이력의 당시 기준 버전 보존 |
| 입력 | 사진첩 스크린샷, 카메라 촬영, 제품명 검색/직접 입력 경로 | 권한 거부·촬영 취소·이미지 손상·제품 미검색 대응 |
| 전송 확인 | 자르기·미리보기·전송 범위 명시 후 확인 | 주소·주문번호 등이 보이면 다시 자르도록 안내 |
| 추출 확인 | 원재료 원문과 추출 결과를 보여주고 수정 가능 | 판독 실패·누락 의심 시 판정 보류 |
| 결과 | 불일치 성분, 표시 위치, 확인 불가 항목, 당시 기준, 근거 출처 | `기준 내`를 제품 안전·섭취 적합성 보증으로 오인시키지 않음 |
| 이력 | 이전 결과와 당시 기준 보기, 재확인 | 타 사용자 접근 차단·삭제 기능 |

**기준 설정은 `피해요 / 알려만줘요` 2단계로 표시한다 (2026-09-18 사용자 결정).** `피해요`는 원재료 발견 시 표시 위치와 관계없이 기준 불일치에 포함한다. `알려만줘요`는 발견 사실을 안내하되 기준 불일치에는 포함하지 않는다. 선택하지 않은 원재료는 개인 기준 대조 대상에서 제외하며 별도의 세 번째 설정으로 표시하지 않는다. 확인 불가·판독 실패는 두 설정 모두 별도 안내한다.

결과 문구는 내부 PRD의 **“피해요로 설정한 성분이 발견됨/발견되지 않음”** 수준으로 유지하고, 알려만줘요 항목은 따로 안내한다. “안전”, “위험”, “추천”, 질병·건강 영향 판단을 표시하지 않는다. 라벨의 원재료 순서는 위치 정보로만 쓰며 양이나 허용 여부를 결정하는 데 사용하지 않는다. 식품 정보 앱의 정책 분류·표현 검수는 제출 전 [서비스 오픈 정책](https://developers-apps-in-toss.toss.im/intro/guide)과 내부 [법무·표현 설계](docs/02_법무-표현설계.md)를 다시 대조한다.

## 5. 무료 서비스 운영

**이 서비스는 무료로만 운영한다(2026-09-18 결정).** 개발자가 사업자가 아니어서 앱인토스에서 유료 상품을 팔 수 없다.
가격·결제 수단·카드 정보를 받지 않고, 구독·인앱 결제·광고를 붙이지 않는다. 사업자 등록을 하게 되면 그때 다시 검토한다.

- **상한은 월 예산이다.** 월 2만원에 닿으면 서버가 사진 읽기를 막고, 그동안 서비스를 닫고 다시 준비한다.
  기기당 하루 10장, IP당 10분 4건을 함께 둔다. 사용량은 Supabase 표에 쌓고 공개 키로는 접근할 수 없다. 화면에는 남은 횟수를 표시한다.
  같은 사진은 다시 보내지 않는다(기기에 기억).
- 비용 실측(2026-09-18): `gemini-3.1-flash-lite` + `MEDIA_RESOLUTION_LOW` 로 **한 장 약 0.56원**.
  월 2만원이면 약 3.5만 장이고, 기기당 하루 10장 기준으로 하루 1,000명이 2장씩 쓰면 월 약 2.8만원이라 상한에 먼저 닿는다.
- 상한에 닿으면 정할 것 — 하루 한도를 10장에서 더 조일지, 예산을 올릴지, 서비스를 접을지.
- **광고는 붙이지 않는다(2026-09-18 확인).** 외부 광고 네트워크 연동은 정책상 금지이고, 쓸 수 있는 것은 앱인토스가 주는
  전면형·보상형·배너 광고뿐이다. 그런데 **인앱 광고 연동에도 사업자 등록이 필요**하고, **광고 카테고리를 제외하는 기능은 문서에 없다.**
  식품 광고를 완전히 뺄 수 있는지 확인되지 않으므로, 도입하려면 사업자 등록과 토스 문의가 먼저다. 자세한 것은 `docs/02` §7-6.
- **사업자 등록 없이 무료 미니앱은 낼 수 있지만, 토스 로그인·인앱 결제를 켜려면 콘솔의 사업자 등록이 필수**다.
  따라서 익명키 기반으로만 운영하고, 사용자 기준·이력은 기기에만 저장한다. [사업자 등록 안내](https://developers-apps-in-toss.toss.im/guide/operation/register-business).
- AI 사용에 따른 이미지 처리·국외 처리 가능성, 보관 여부, 추출 텍스트의 저장 범위, 문의·삭제 경로를 개인정보처리방침과 화면 안내에 일치시킨다. 개인정보처리방침·약관·운영자 정보·문의 채널을 콘솔 등록 전에 준비한다.

## 6. 출시 준비 순서와 통과 기준

| 순서 | 작업 | 통과 기준 |
|---|---|---|
| 0 | 앱인토스 콘솔 워크스페이스·미니앱 등록, `appName` 확정 | 앱 이름·아이콘·카테고리·설명·연락처·개인정보처리방침·앱 내 기능 등록 내용이 일치 |
| 1 | SDK 3.x WebView 최소 프로젝트 및 자체 API 연결 | AIT Devtools에서 진입·뒤로가기·CORS·권한·에러 화면 확인 |
| 2 | 익명 사용자 검증, 세션, 기준·이력 접근 제어 | 익명·A·B 계정 교차 접근 차단, 종료 후 재진입 이력 유지 |
| 3 | 사진·OCR·원재료 확인·대조 화면 완성 | 실물 라벨 20장 이상에서 **제한 성분을 놓친 채 `발견되지 않음`이라고 표시한 사례 0건**; 모호하면 보류 |
| 4 | 성분·근거 데이터 정리 | 내부 검증 보고서의 남은 F6~F13, 원재료 식별 문제 해결 또는 해당 데이터 사용자 노출 차단 |
| 5 | 개인정보·표현·운영 점검 | 저장·삭제·문의·AI 고지·출처·반려 가능 문구와 콘솔 정보 검토 |
| 6 | `.ait` 빌드·업로드, 토스 앱 QR 테스트 | Android·iOS 실기기에서 딥링크, 카메라·앨범, 뒤로가기, 키보드, 네트워크, CORS, 이력 재진입 통과 |
| 7 | 비게임 체크리스트 완료 후 검토 요청 | 최소 1회 콘솔 테스트 완료, 번들 압축 해제 기준 **100MB 이하**, 반려 항목 없음 |
| 8 | 승인 후 출시·모니터링 | 콘솔에서 출시, 실제 Origin·오류율·응답 지연·사용자 신고 확인, 문제 시 이전 번들로 롤백 |

콘솔 테스트를 1회 이상 완료해야 검토 요청할 수 있다. 검토는 영업일 기준 최대 3일, 카테고리에 따라 7일 이상 걸릴 수 있으며, 승인을 받은 뒤 콘솔에서 별도로 출시해야 한다. 수정 번들은 다시 검토한다. [토스 앱 테스트](https://developers-apps-in-toss.toss.im/guide/operation/toss), [출시 절차](https://developers-apps-in-toss.toss.im/guide/operation/deploy), [비게임 체크리스트](https://developers-apps-in-toss.toss.im/checklist/app-nongame).

## 7. 구현 전에 결정하거나 실험할 항목

1. **서버 mTLS 가능 여부:** 아래 검증 절차를 **배포된 Supabase Edge Function**에서 완료한다. 검증 전까지 지원 여부는 미확정이다. 실패가 재현될 때만 전용 게이트웨이를 비교한다.
2. **익명키 기반 이력 범위:** 기기 변경·앱 재설치 때 기기 저장이 어떻게 되는지 실기기로 확인하고, 화면 안내와 맞춘다.
3. **결과 검수 기준:** 원재료 OCR의 누락, 복합원재료, 동의어 매칭, 피해요/알려만줘요 설정별 처리를 실물 라벨로 검증한다. 피해요는 앞·중간·뒤 위치와 관계없이 불일치로 집계하고, 알려만줘요는 발견 사실을 안내하되 불일치에서 제외하는지 확인한다. 불확실한 근거는 출시 화면에서 제외한다.
4. **월 예산 상한 운영:** 2만원에 닿았을 때 하루 한도를 조일지·예산을 올릴지·닫을지를 미리 정해 둔다.
5. **정책 재확인:** 출시 신청 직전에 서비스 오픈 정책, 비게임 가이드, SDK 릴리즈 노트, 실제 QR Origin을 다시 확인한다. 문서 간 CORS·설정 예시가 충돌하는 지점은 실기기 결과를 우선한다.

### 서버 mTLS 검증 절차

현재 상태(2026-09-17): **미검증**. 이 저장소에는 토스 콘솔에서 발급한 클라이언트 인증서·개인 키와 연결된 Supabase 프로젝트가 없으므로 실제 호출 성공을 주장할 수 없다. 토스 서버 API를 쓰지 않는 로컬 화면·카메라 시제품에는 mTLS가 필요하지 않다. 익명키 검증 API를 붙일 때부터 필요하다.

1. 토스 콘솔에서 서버 API용 클라이언트 인증서와 개인 키를 발급하고, 테스트 기기의 `User.getAnonymousKey()`로 유효한 익명키를 얻는다. 인증서·키·익명키는 저장소나 로그에 남기지 않는다. [토스 서버 API 설정](https://developers-apps-in-toss.toss.im/documentation/integration/server-api), [익명키 검증 API](https://developers-apps-in-toss.toss.im/api/user-key).
2. 인증서와 키를 Supabase 프로젝트 **Secrets**에 PEM 문자열로 등록한다. 배포할 최소 진단 함수에서 `Deno.createHttpClient({ cert, key })`로 클라이언트를 만들고, `fetch(url, { client, method: 'POST', headers: { 'x-anon-key': anonKey } })`로 `https://apps-in-toss-api.toss.im/api-partner/v1/apps-in-toss/users/anon-key/verify`를 호출한다. 현재 [Deno 문서](https://docs.deno.com/api/deno/fetch/)의 옵션 이름은 `cert`·`key`이고, `fetch`에 `client`를 전달해야 한다. 진단 함수는 별도 인증으로 보호하고 응답에는 HTTP 상태, `resultType`, 오류 종류만 남긴다.
3. 같은 인증서·키·익명키로 개발 PC 또는 일반 서버에서 `curl.exe --cert <인증서 파일> --key <키 파일> -H "x-anon-key: <익명키>" -X POST <위 URL>`을 실행해 대조한다(Windows PowerShell 기준). 이 대조군도 실패하면 인증서·키 쌍, 인증서 상태, 익명키, 호출 도메인부터 확인한다.
4. **합격:** 배포된 Edge Function에서 TLS 연결이 성립하고 토스가 `resultType: SUCCESS`와 `success: true`를 반환한다. HTTP 200이어도 `resultType: FAIL`이면 호출 경로의 TLS 성립 여부와 비즈니스 오류를 분리해 기록한다. `CertificateRequired`나 TLS 핸드셰이크 오류가 나면 클라이언트 인증서 전달 문제로 분류한다. 로컬 실행 성공만으로 호스팅 환경 합격 처리하지 않는다.
5. 배포 환경에서 실패하고 대조군이 성공하면 함수의 `cert`·`key` 옵션, PEM 개행, `fetch`의 `client` 전달, 호스팅 런타임 버전을 점검한 뒤 재시험한다. 그래도 실패하면 Supabase 지원 답변 또는 재현 기록을 근거로 mTLS 전용 서버를 선정하고 월 기본료·요청량 비용·인증서 보관/교체·장애 알림·접근 제어 운영 담당을 확정한다. Supabase Edge Runtime 소스에는 [`Deno.createHttpClient` 노출 변경](https://github.com/supabase/edge-runtime/pull/515)이 병합되었지만, 이것만으로 실제 호스팅 환경의 토스 연동을 보증하지는 않는다.

## 8. 공식 자료

- [앱인토스 서비스 오픈 정책](https://developers-apps-in-toss.toss.im/intro/guide) · [비게임 출시 가이드](https://developers-apps-in-toss.toss.im/checklist/app-nongame)
- [API·SDK 개요](https://developers-apps-in-toss.toss.im/documentation) · [SDK 3.x 마이그레이션](https://developers-apps-in-toss.toss.im/documentation/integration/sdk-3.x) · [릴리즈 노트](https://developers-apps-in-toss.toss.im/release-note)
- [서버 API 이용](https://developers-apps-in-toss.toss.im/documentation/integration/server-api) · [익명키 검증 API](https://developers-apps-in-toss.toss.im/api/user-key) · [IAP 구독](https://developers-apps-in-toss.toss.im/documentation/common/monetization/iap/in-app-subscription)
- [미니앱 등록](https://developers-apps-in-toss.toss.im/guide/operation/console-workspace) · [토스 앱 테스트](https://developers-apps-in-toss.toss.im/guide/operation/toss) · [미니앱 출시](https://developers-apps-in-toss.toss.im/guide/operation/deploy)
